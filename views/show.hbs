<div class="row mb-4">
  <div class="col-12">
    <div class="card">
      <div class="card-body d-flex flex-column">
        <div>
          <div class="d-flex flex-row justify-content-between align-items-center">
            <a class="btn btn-primary mb-2" data-bs-toggle="tooltip" data-bs-placement="top" data-bs-title="Ver Perfil"
              href="/profile/{{userProfile._id}}">{{userProfile.name}}</a>
            <img src="{{userProfile.avatar}}" alt="Avatar {{userProfile.name}}" style="max-width: 70px;">
          </div>
        </div>
        <h2 class="text-center">{{post.title}}</h2>
        <img class="img-fluid justify-self-center rounded" src="{{post.image}}" alt="Portada de {{post.title}}"
          style="height: 450px;">

        <div id="" class="mt-5 mb-5">
          {{{post.body}}}
        </div>

        {{> share}}

      </div>
    </div>
  </div>


  {{> listComments}}

  {{#if user}}
    {{> createComment}}
  {{/if}}

</div>

<script>
  const whatsapp = document.querySelector('.whatsapp')
  const telegram = document.querySelector('.telegram')
  const twitter = document.querySelector('.twitter')
  const pageUrl = location.href
  const message = 'Mira este post! '
  const apiwsp = `https://wa.me/?text=${message}. ${pageUrl}`
  const apitwitter = `https://twitter.com/intent/tweet?text=${pageUrl}. ${message}`
  const apitelegram = `https://t.me/share/url?url=${pageUrl}&text=${message}`

  whatsapp.addEventListener('click', () => {
    window.open(url = apiwsp, target = 'blank')
  })
  twitter.addEventListener('click', () => {
    window.open(url = apitwitter, target = 'blank')
  })
  telegram.addEventListener('click', () => {
    window.open(url = apitelegram, target = 'blank')
  })

</script>