<div class="row justify-content-center">
  <div class="col-8">


    <form action="/posts/edit/{{postEdit._id}}?_method=POST" method="POST" class="card border-secondary">
      <div class="card-header">Editar Post</div>

      <div class="card-body">

        <div class="form-group">
          <label for="title" class="form-label">Título</label>
          <input type="text" class="form-control" id="title" name="title" value="{{postEdit.title}}" />
        </div>

        <div class="form-group">
          <label for="body" class="form-label">Contenido</label>
          <textarea class="form-control" id="body" name="body" rows="3">{{postEdit.body}}</textarea>
        </div>

      <button class="btn btn-primary mt-4">
        Enviar
      </button>
  </div>
  </form>

</div>
</div>